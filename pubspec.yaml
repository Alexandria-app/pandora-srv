name: example
description: "Dummy pubspec to generate the server archives"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  serious_python: ^0.7.0

dev_dependencies:
  build_runner: ^2.4.10

  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
